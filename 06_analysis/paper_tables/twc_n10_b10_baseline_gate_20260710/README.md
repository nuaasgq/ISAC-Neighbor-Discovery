# N=10/B=10 baseline learnability gate

`paired_3seed_summary.csv` compares the same first three scenario seeds across uniform random, Wang2025 table exchange, and the rule-ISAC table method. `completed_10seed_subset.csv` contains only the two methods that completed before the original command reached its hard timeout.

These are diagnostic gate results, not paper performance evidence. Raw episode rows remain under the ignored `05_simulation/results_raw/` directories listed in `06_analysis/twc_n10_b10_baseline_gate_20260710.md`.
